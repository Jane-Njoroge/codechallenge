function speedCheck(speed) {const speedLimit=70
    if (speeed <= speedLimit) {console.log("ok")
    }else {
let demeritPoints;
demeritPoints=
Math.floor((speed-speedLimit)/5)
if(demeritPoints>12) {
    console.log("License suspended")
}else {
    console.log(points; ${demeritPoints})
}
}
}